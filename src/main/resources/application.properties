# ----------------------------------------
# Uygulama Temel Ayarlar?
# ----------------------------------------
spring.application.name=rentacar

# ----------------------------------------
# Veritaban? Ba?lant?s?
# ----------------------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/rentacar_db?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username=root
spring.datasource.password=Hakan8823

# E?er MySQL'in portu de?i?ikse (örne?in 3307), url'yi ona göre de?i?tir.

# ----------------------------------------
# JPA / Hibernate Ayarlar?
# ----------------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ----------------------------------------
# Karakter Seti
# ----------------------------------------
spring.datasource.hikari.connection-init-sql=SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci
